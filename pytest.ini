[pytest]
testpaths = tests
markers =
    cleanroom_e2e: cleanroom smoke test that exercises a real skill end-to-end via `claude -p --plugin-dir` (uses the CLI's existing auth — subscription login or API key; skips when `claude` is not on PATH or not logged in; costs ~$0.05/run in Haiku, ~30–60s wall)
